#!/usr/bin/python3
import math         # import pulls the function out of math to use
number = input("Give me a number: ")
print(math.ceil(float(number)))         # ciel rounds up the decimals (in this case the integer math), float() converts a value into a decimal number